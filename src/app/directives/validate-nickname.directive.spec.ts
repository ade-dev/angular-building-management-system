import { ValidateNicknameDirective } from './validate-nickname.directive';

describe('ValidateNicknameDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateNicknameDirective();
    expect(directive).toBeTruthy();
  });
});
